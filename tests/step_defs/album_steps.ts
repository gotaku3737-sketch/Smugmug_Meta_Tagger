import { Given, When, Then } from '@cucumber/cucumber';

Given('the user is on the Galleries page', function () {
  return 'pending';
});

When('the user clicks "Sync Albums"', function () {
  return 'pending';
});

Then('the app should fetch the list of albums from SmugMug', function () {
  return 'pending';
});

Then('the local database should be updated with the album metadata', function () {
  return 'pending';
});

Then('the Galleries page should display the albums', function () {
  return 'pending';
});

Given('the app has synced albums', function () {
  return 'pending';
});

When('the user clicks "Download" on an album card', function () {
  return 'pending';
});

Then('the app should download small thumbnails for that album', function () {
  return 'pending';
});

Then('the thumbnails should be saved locally', function () {
  return 'pending';
});

Then('the album should display the thumbnails for browsing', function () {
  return 'pending';
});
